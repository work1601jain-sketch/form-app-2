# Backend
MONGO_URI=mongodb://127.0.0.1:27017
MONGO_DATABASE=formsdb
PORT=3001

# Frontend (Vite)
# When deploying frontend and backend to separate hosts, set this to the backend URL
VITE_BACKEND_URL=

# Optional MongoDB Atlas Data API (if using Data API instead of local backend)
VITE_MONGODB_DATA_API_URL=
VITE_MONGODB_DATA_API_KEY=
VITE_MONGODB_DATA_SOURCE=Cluster0
VITE_MONGODB_DATABASE=formsdb
